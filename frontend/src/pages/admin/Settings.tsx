export function Settings() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-ink">Settings</h1>
      <p className="mt-2 text-ink-muted">
        Admin settings (platform config, notification preferences, etc.) haven't
        been scoped yet. This is a placeholder until we design that page
        together.
      </p>
    </div>
  );
}
